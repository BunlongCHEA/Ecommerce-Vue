<template>
  <div class="see-more-container" v-if="showButton">
    <button @click="$emit('click')" class="see-more-button">See More</button>
  </div>
</template>

<script setup>
const props = defineProps({
  showButton: { type: Boolean, required: true },
})
</script>

<style scoped>
.see-more-container {
  margin-top: 0.5rem;
}

.see-more-button {
  padding: 0.4rem 0.8rem;
  background-color: #007bff;
  color: white;
  border: none;
  border-radius: 4px;
  font-size: 0.85rem;
  cursor: pointer;
}

.see-more-button:hover {
  background-color: #0056b3;
}
</style>
